- 文档型 Markdown 使用「日期前缀文件名」（如 `2026-07-17-course-video-knowledge-design.md`）+ 顶部元信息块（For agentic workers / Goal / Tech Stack / Spec）统一标注，便于 superpowers 子代理解析。
- 可执行计划采用 checklist 语法 `- [ ] Step N:` 描述步骤，并在每个 Task 末尾给出 MCP 调用验证断言（如 `readNoSqlDatabaseContent` 查 videos 集合预期 total: 30）。
- 原型页面以单一 index.html 承载多屏手机模型，通过 `.phone-item` + `.phone` + `.screen` 嵌套复用同一套状态栏/Tabbar 骨架，新增页面只需复制一个 phone-item 区块。